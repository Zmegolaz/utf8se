{
  "name": "utf8se",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A modular collection of browser-based UTF-8 / Unicode tools.",
  "scripts": {
    "serve": "python3 -m http.server 8765 --directory public",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:e2e": "playwright test",
    "test:py": "python3 -m unittest discover -s tests/python -p '*_test.py' -v",
    "test:all": "npm run test:unit && npm run test:py && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
